Basic code for CUDA and OpenGL interoperability

For building, go to build folder, and then type down ```cmake ../```

For compiling, after building, open VS solution, and compile the project, or
use the makefile generated

For running, you can use VS solution, or execute directly the generated .exe